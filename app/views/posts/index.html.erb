<div class="row1">
	<div class="col-12">
	</div>
</div>
<div class="row row2 grey_background">
	<h1 class="site_name">My Posts</h1>
	<% @posts.each do |post| %>
		<%= render post %>
		<% post.comments.each do |comment| %>
			<%= render comment %>
		<% end %>
	<% end %>
</div>
